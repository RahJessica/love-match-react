import FormUser from './components/formUser';
import SplitText from './components/SplitText';

export default function App() {
  return (
    <main className="w-full h-screen flex items-center justify-center bg-gradient-to-r from-[#ff758f] via-[#ffb3c1] to-[#ff758f]">
      <FormUser/>
    </main>
  );
}
